# Personal, machine-specific Claude context (see CLAUDE.local.md.example)
CLAUDE.local.md

# Secrets
.env
.env.*
!.env.example

# Node / Next
node_modules/
.next/
out/
dist/

# OS
.DS_Store

# Claude Code per-session marker (used by the Stop hook to scope plan saves)
.claude/.session-start
